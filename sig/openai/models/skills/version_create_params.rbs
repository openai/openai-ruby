module OpenAI
  module Models
    module Skills
      type version_create_params =
        {
          skill_id: String,
          ?default: bool,
          ?files: OpenAI::Models::Skills::VersionCreateParams::files,
          ?request_options: OpenAI::request_opts
        }

      class VersionCreateParams < OpenAI::Internal::Type::BaseModel
        extend OpenAI::Internal::Type::RequestParameters::Converter
        include OpenAI::Internal::Type::RequestParameters

        attr_accessor skill_id: String

        # Whether to set this version as the default.
        attr_reader default: bool?

        # Whether to set this version as the default.
        def default=: (bool) -> bool

        # Skill files to upload (directory upload) or a single zip file.
        #
        # `String`, `StringIO`, and pathless `IO` inputs are sent with generic upload
        # metadata. Use `OpenAI::FilePart` when you need to override the filename or
        # content type.
        attr_reader files: OpenAI::Models::Skills::VersionCreateParams::files?

        # Skill files to upload (directory upload) or a single zip file.
        #
        # `String`, `StringIO`, and pathless `IO` inputs are sent with generic upload
        # metadata. Use `OpenAI::FilePart` when you need to override the filename or
        # content type.
        def files=: (
          OpenAI::Models::Skills::VersionCreateParams::files
        ) -> OpenAI::Models::Skills::VersionCreateParams::files

        # @param default
        #   Whether to set this version as the default.
        # @param files
        #   Skill files to upload (directory upload) or a single zip file.
        #
        #   `String`, `StringIO`, and pathless `IO` inputs are sent with generic upload
        #   metadata. Use `OpenAI::FilePart` when you need to override the filename or
        #   content type.
        def initialize: (
          skill_id: String,
          ?default: bool,
          ?files: OpenAI::Models::Skills::VersionCreateParams::files,
          ?request_options: OpenAI::request_opts
        ) -> void

        def to_hash: -> {
          skill_id: String,
          ?default: bool,
          ?files: OpenAI::Models::Skills::VersionCreateParams::files,
          ?request_options: OpenAI::RequestOptions
        }

        type files =
          ::Array[OpenAI::Internal::file_input] | OpenAI::Internal::file_input

        module Files
          extend OpenAI::Internal::Type::Union

          def self?.variants: -> ::Array[OpenAI::Models::Skills::VersionCreateParams::files]

          StringArray: OpenAI::Internal::Type::Converter
        end
      end
    end
  end
end
